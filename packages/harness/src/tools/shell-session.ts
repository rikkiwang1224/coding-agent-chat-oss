/**
 * @deprecated Use `LocalEnvironment` (and the `ExecutionEnvironment` interface)
 * from `../execution/` instead. This alias is retained for backward
 * compatibility with existing imports of `ShellSession`.
 */
export { LocalEnvironment as ShellSession } from "../execution/local-environment.js";
